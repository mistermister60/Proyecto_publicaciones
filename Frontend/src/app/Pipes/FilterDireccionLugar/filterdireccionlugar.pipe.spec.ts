import { FilterdireccionlugarPipe } from './filterdireccionlugar.pipe';

describe('FilterdireccionlugarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdireccionlugarPipe();
    expect(pipe).toBeTruthy();
  });
});
