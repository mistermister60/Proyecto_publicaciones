import { FilterperfilempresaPipe } from './filterperfilempresa.pipe';

describe('FilterperfilempresaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterperfilempresaPipe();
    expect(pipe).toBeTruthy();
  });
});
