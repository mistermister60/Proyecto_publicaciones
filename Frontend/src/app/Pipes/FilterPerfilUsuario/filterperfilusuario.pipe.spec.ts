import { FilterperfilusuarioPipe } from './filterperfilusuario.pipe';

describe('FilterperfilusuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterperfilusuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
