import { FilterempresaPipe } from './filterempresa.pipe';

describe('FilterempresaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterempresaPipe();
    expect(pipe).toBeTruthy();
  });
});
