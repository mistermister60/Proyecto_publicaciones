import { FiltercoloniaPipe } from './filtercolonia.pipe';

describe('FiltercoloniaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercoloniaPipe();
    expect(pipe).toBeTruthy();
  });
});
