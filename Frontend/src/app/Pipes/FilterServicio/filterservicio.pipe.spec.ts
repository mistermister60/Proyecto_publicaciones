import { FilterservicioPipe } from './filterservicio.pipe';

describe('FilterservicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterservicioPipe();
    expect(pipe).toBeTruthy();
  });
});
