import { FilterpublicacionempresaPipe } from './filterpublicacionempresa.pipe';

describe('FilterpublicacionempresaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpublicacionempresaPipe();
    expect(pipe).toBeTruthy();
  });
});
