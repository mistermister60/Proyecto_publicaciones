import { FilterpublicacionPipe } from './filterpublicacion.pipe';

describe('FilterpublicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpublicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
